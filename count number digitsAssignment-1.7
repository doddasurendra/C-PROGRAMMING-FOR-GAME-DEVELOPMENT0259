#include <stdio.h>

int main() {
    int num, count = 0;
    
    printf("Enter a number: ");
    scanf("%d", &num);
    
    count = (int)log10(num) + 1;
    
    printf("Total digits: %d", count);
    
    return 0;
}
